README.txt

To work correctly, ACR122U needs to be installed with NFC reader. Another terminal must be open, with "pcsc_scan" running.
Place card on scanner, and if the reader sees card, open another terminal to run python code.